# Production/Integration build: REQUIRED
# Can include a path prefix if backend is mounted under a subpath (e.g., /meta)
VITE_API_URL=https://backend.internal.example/meta

# Use real backend in prod
VITE_USE_MOCK=0
VITE_AUTH_DEV_BYPASS=0
