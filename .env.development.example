# Local dev: no API URL needed if you use Vite dev proxy or same-origin backend
# VITE_PROXY_TARGET can be used to proxy /api -> backend
# VITE_PROXY_TARGET=https://localhost:8443

VITE_USE_MOCK=0
VITE_AUTH_DEV_BYPASS=0
